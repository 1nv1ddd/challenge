# Контрольные факты PolarLine (День 22)

Учебный фрагмент для проверки RAG: часть якорей **уникальна** для этого файла (не копируется в `00_polarline_handbook.md`).

## Релиз и набор проверки

- Код релиза учебного стенда: **REL-DAY22-09** (ветка `eval/day22`, только документация).
- Набор **PolarEval Kit** версии **2.1** хранит эталонные вопросы в `data/rag_eval/golden_questions.json`.

## Расшифровки для чата

- В этом приложении аббревиатура **RAG** в пользовательском интерфейсе означает **Retrieval-Augmented Generation** (извлечение релевантных фрагментов из локального индекса и подмешивание их в запрос к модели).
